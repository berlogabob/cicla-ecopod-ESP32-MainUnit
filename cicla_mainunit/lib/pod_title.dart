import 'package:flutter/material.dart';

class PodTitle extends StatelessWidget {
  const PodTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Pod_01_base_01',
      style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
    );
  }
}
